/**
 * Tag categories — must match server ALLOWED_CATEGORIES (tags.js).
 */
export const TAG_CATEGORIES = ['Your points', "Opponent's points"];

/** YouTube video id for the seeded demo film (see server/seed-demo.sql) */
export const DEMO_YOUTUBE_VIDEO_ID = 'EfNY05H7WoY';
